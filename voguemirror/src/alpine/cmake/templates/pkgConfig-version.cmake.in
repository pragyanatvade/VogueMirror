# generated from alpine/cmake/template/pkgConfig-version.cmake.in
set(PROJECT_VERSION "@PROJECT_VERSION@")

set(PROJECT_VERSION_EXACT False)
set(PROJECT_VERSION_COMPATIBLE False)

if("${PROJECT_FIND_VERSION}" VERSION_EQUAL "${PROJECT_VERSION}")
  set(PROJECT_VERSION_EXACT True)
  set(PROJECT_VERSION_COMPATIBLE True)
endif()

if("${PROJECT_FIND_VERSION}" VERSION_LESS "${PROJECT_VERSION}")
  set(PROJECT_VERSION_COMPATIBLE True)
endif()
