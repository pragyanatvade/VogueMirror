#!/usr/bin/env zsh
# generated from alpine/cmake/templates/setup.zsh.in

ALPINE_SHELL=zsh
_ALPINE_SETUP_DIR=$(builtin cd "`dirname "$0"`" && pwd)
emulate sh # emulate POSIX
. "$_ALPINE_SETUP_DIR/setup.sh"
emulate zsh # back to zsh mode
